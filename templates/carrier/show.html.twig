{% extends 'base.html.twig' %}

{% block title %}Carrier{% endblock %}

{% block body %}
    <h1>Carrier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carrier.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ carrier.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ carrier.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ carrier.price }}</td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('carrier_edit', {'id': carrier.id}) }}">Modifier</a>

         {{ include('carrier/_delete_form.html.twig') }}

    <a href="{{ path('carrier_index') }}">Retourner à la liste</a>

    

    
{% endblock %}
